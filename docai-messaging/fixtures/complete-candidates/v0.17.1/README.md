# DocAI Messaging Complete Candidate 0.17.1

This directory is a work-in-progress complete generator surface candidate. It is not a release artifact or an implementation-target publication.

The current baseline contains a whole-set-valid full/compact profile pair and the explicit projection-input manifest used to stamp both roots. Task 12 will extend this baseline with the advanced structures and source traceability required by the complete candidate plan. Task 13 will add the focused corpus, coverage matrix, and evidence gates.

Validate the current pair without modifying it:

```sh
node docai-messaging/tools/check-complete-fixtures.mjs
```
