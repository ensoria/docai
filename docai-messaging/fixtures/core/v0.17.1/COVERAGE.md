# Compatibility Core Coverage Matrix

This matrix maps the DocAI Messaging `0.17.1` Compatibility Core corpus requirements in README §8 to versioned evidence. It is a release-review artifact, not a substitute for the fixture checker or source-aware human review.

## Matrix Contract

Each `R8-CORE-*` row represents one independently reviewable Core corpus requirement. Requirement IDs remain stable if README prose moves. Fixture entries are `cases.json` case IDs; that manifest is the authoritative mapping from an ID to its file and expected result. `Source / derivation` names authoritative input or, for context-free grammar cases, the normative README derivation instead of inventing a source artifact.

Status values are:

- `covered`: every cited fixture, rule, and checker exists and currently passes.
- `partial`: evidence exists, but another matrix row or fixture group is still required before the requirement is complete.
- `pending`: the requirement has not yet been mapped completely.

Cascade diagnostics do not satisfy a Rule column. Invalid fixtures must identify one primary rule concern and pass the corpus one-invalidity audit.

## Core Requirements

| ID | README §8 requirement | Source / derivation | Valid fixture cases | Invalid fixture cases | Primary rule IDs | Checker tests | Status |
|---|---|---|---|---|---|---|---|
| `R8-CORE-001` | Publish a versioned valid full-profile set and focused valid and invalid fixtures for every Compatibility Core structure before advertising Core as an implementation target. | `source/projection-input-manifest.json`; `source/storefront.asyncapi.json`; `source/storefront-behavior.json`; `SOURCE-TRACEABILITY.md` | `core-valid-full-set` and all mapped focused valid cases | All mapped focused invalid cases | All mapped Core `DM-*` rules | `document-set.test.mjs`: `validates the Task 8 contract-complete full document set`; `core-corpus.test.mjs`: all `executes the Task 9 ... corpus` tests and `audits every Task 9 invalid fixture as one primary concern` | `partial` |
| `R8-CORE-002` | Cover valid and invalid opening metadata and identity trailers. | README §3.1 opening-metadata grammar; README §3.2 identity grammar; main-set derivations in `SOURCE-TRACEABILITY.md` → `Document Metadata and Identity` | `metadata-canonical-extensions-and-escapes`; `identity-whole-set-valid`; `core-valid-full-set` | `metadata-missing-standard-key`; `metadata-format-version-invalid`; `metadata-profile-invalid`; `metadata-perspective-empty`; `metadata-coverage-invalid`; `metadata-knowledge-invalid`; `identity-trailer-missing`; `identity-trailer-malformed-projection-digest` | `DM-META-001`; `DM-ID-001` | `core-corpus.test.mjs`: `executes the Task 9 metadata and sentence focused corpus`, `executes the Task 9 identity focused corpus`; `identity.test.mjs`: `parses only the fixed root and non-root identity trailer shapes` | `covered` |
| `R8-CORE-003` | Cover valid lowercase metadata extension names; invalid uppercase, empty-suffix, and disallowed-punctuation names; duplicate standard and extension keys; value-only escaping; and rejection of an unknown non-`x-` key for the same or an older supported declaration. | README §3.1 metadata name, ordering, uniqueness, and value-only escape grammar | `metadata-canonical-extensions-and-escapes` | `metadata-extension-uppercase`; `metadata-extension-empty-suffix`; `metadata-extension-disallowed-punctuation`; `metadata-duplicate-standard-key`; `metadata-duplicate-extension-key`; `metadata-unknown-escape`; `metadata-trailing-backslash`; `metadata-unknown-non-extension-key` | `DM-META-001`; `DM-META-004` | `core-corpus.test.mjs`: `executes the Task 9 metadata and sentence focused corpus`; `metadata.test.mjs`: extension, duplicate-key, escape, and unknown-standard-key tests | `covered` |
| `R8-CORE-004` | Cover deterministic `set_digest` recomputation, closed-root membership, path and content binding, short-ID derivation, mixed-set rejection, and task-scoped identity checking without whole-set retrieval. | `source/projection-input-manifest.json`; README §3.2 identity derivation; main-set derivations in `SOURCE-TRACEABILITY.md` → `Document Metadata and Identity` | `identity-whole-set-valid`; `identity-task-scoped-stale-digest`; `core-valid-full-set` | `identity-task-scoped-stale-digest-whole-set-invalid`; `identity-closed-root-extra-file`; `identity-set-short-id-invalid`; `identity-projection-short-id-invalid`; `identity-format-version-mixed`; `identity-profile-mixed`; `identity-perspective-mixed`; `identity-set-id-mixed`; `identity-projection-id-mixed` | `DM-ID-002`; `DM-ID-003`; `DM-ID-004`; `DM-ID-005`; `DM-ID-006`; `DM-ID-007`; `DM-ID-008`; `DM-ID-009` | `core-corpus.test.mjs`: `executes the Task 9 identity focused corpus`; `identity.test.mjs`: digest vector, `SELF`, short-ID, and path/membership/content binding tests; `document-set.test.mjs`: closed-root, mixed-set, and `task-scoped validation checks handles without recomputing the whole set` tests | `covered` |
| `R8-CORE-005` | Cover direct and sharded Sources with known and unknown API versions, `Revision=none`, exact-ID selection, `source_refs: all`, overlapping ranges, duplicate and missing rows, false-positive loads, and load-all fallback. | `source/projection-input-manifest.json`; `source/storefront.asyncapi.json`; README §3.2 Sources and Source Shards grammar; `SOURCE-TRACEABILITY.md` → `INDEX Fact Matrix` | `sources-direct-known-revision-none-valid`; `sources-direct-unknown-api-valid`; `sources-sharded-overlap-valid`; `sources-sharded-cycle-valid`; `core-valid-full-set` | `sources-direct-columns-invalid`; `sources-direct-duplicate-id-invalid`; `sources-revision-sha-invalid`; `sources-shard-bounds-invalid`; `sources-sharded-duplicate-row-invalid`; `sources-sharded-missing-row-invalid`; `sources-source-refs-missing-invalid`; `sources-unknown-marker-missing-invalid`; `sources-unknown-conventions-repeat-missing-invalid` | `DM-SRC-001`; `DM-SRC-002`; `DM-SRC-003`; `DM-SRC-004`; `DM-SRC-005`; `DM-SRC-006`; `DM-SRC-007` | `core-corpus.test.mjs`: `executes the Task 9 Sources focused corpus and fixes retrieval facts`; `document-set.test.mjs`: `DM-SRC-001`–`DM-SRC-007` source catalog, shard routing, and fixed-point tests | `covered` |
| `R8-CORE-006` | Cover contract-complete roots that are and are not implementation-ready for a Core reader, including a required workflow outside Core; vary target-runtime capabilities for the same operation and task without blocking on an unused alternative; and vary exact source-adapter support without making adapters an ordinary-reader requirement. | `source/projection-input-manifest.json`; `source/storefront.asyncapi.json`; `source/storefront-behavior.json`; README §3 implementation-readiness rules | `core-valid-full-set`; `implementation-readiness-capability-matrix-valid`; `implementation-readiness-required-workflow-valid`; `contexts-required-supplemental-valid` | `implementation-readiness-projection-invalid`; `implementation-readiness-required-workflow-projection-invalid` | `DM-INC-002`; `DM-INC-003`; `DM-IDX-005` | `core-corpus.test.mjs`: `executes the Task 9 DM-INC-003 implementation-readiness capability matrix`, `executes the Task 9 context focused corpus and fixes selected context paths`; `document-set.test.mjs`: `DM-INC-003 keeps an unrelated marker from blocking selected-operation readiness` | `covered` |
| `R8-CORE-007` | Cover flat operation routing. | `source/storefront.asyncapi.json`; README §3.2 flat Operations grammar; `SOURCE-TRACEABILITY.md` → `INDEX Fact Matrix` | `operations-flat-routing-valid`; `core-valid-full-set` | `operations-flat-table-invalid`; `operations-row-action-invalid` | `DM-IDX-003`; `DM-IDX-004`; `DM-IDX-007` | `core-corpus.test.mjs`: `executes the Task 9 Operations focused corpus and fixes retrieval facts`; `document-set.test.mjs`: flat operation-row and exact retrieval-trace tests | `covered` |
| `R8-CORE-008` | Cover required and supplemental context cells using `none`, exact separators, ASCII ordering, unique paths, eligible and forbidden targets, cross-column duplication rejection, and the Core reader's advanced-structure fallback. | README §3.2 operation context grammar and §6.1 retrieval recipe; context mini-set derivations recorded in the Task 9 context checkpoint | `contexts-required-supplemental-valid`; `contexts-none-path-valid`; `implementation-readiness-required-workflow-valid` | `contexts-duplicate-path-invalid`; `contexts-order-invalid`; `contexts-overlap-invalid`; `contexts-required-reference-invalid`; `contexts-separator-invalid`; `contexts-supplemental-channel-invalid`; `implementation-readiness-required-workflow-projection-invalid` | `DM-IDX-005`; `DM-INC-003` | `core-corpus.test.mjs`: `executes the Task 9 context focused corpus and fixes selected context paths`, `executes the Task 9 DM-INC-003 implementation-readiness capability matrix`; `document-set.test.mjs`: context-list target, separator, ordering, overlap, and retrieval tests | `covered` |
| `R8-CORE-009` | Cover hierarchical operation routing with exact and overlapping bounds, semantic load-all fallback, false-positive loads, and full/compact profile path parity. | README §3.2 Operation Shards grammar and §6.1 retrieval recipe | `operations-hierarchical-overlap-valid`; `operations-profile-path-parity-valid` | `operations-hierarchical-bounds-invalid`; `operations-profile-path-parity-invalid` | `DM-IDX-006`; `DM-IDX-007` | `core-corpus.test.mjs`: `executes the Task 9 Operations focused corpus and fixes retrieval facts`; `document-set.test.mjs`: hierarchical bounds, overlap, false-positive, semantic fallback, and profile-pair tests | `covered` |
| `R8-CORE-010` | Cover the exact context-path `none` sentinel collision while accepting the unambiguous path `workflows/none.md`. | README §3.1 path grammar and §3.2 context-list grammar | `contexts-none-path-valid`; `contexts-required-supplemental-valid` | Not applicable: the exact token `none` is the empty-list sentinel, while `workflows/none.md` is a distinguishable valid path | `DM-PARSE-003`; `DM-IDX-005` | `core-corpus.test.mjs`: `executes the Task 9 context focused corpus and fixes selected context paths`; `paths.test.mjs`: `reports the exact context-path none sentinel collision` | `covered` |
| `R8-CORE-011` | Cover sentinel-like source literals in distinguishable and ambiguous structural positions, including smallest-`unsupported` fallback and mandatory-cell generation failure. | README §3.6 publication safety and §3.7 structural escape rules | `trust-boundary-source-valid`; `publication-safety-source-valid` | `trust-boundary-structure-invalid`; `publication-safety-mandatory-location-invalid` | `DM-TRUST-002`; `DM-TRUST-003` | `core-corpus.test.mjs`: `executes the Task 9 DM-TRUST-001 DM-TRUST-002 DM-TRUST-003 publication and structural-escape corpus`; `document-set.test.mjs`: `DM-TRUST-002 contains structural escape attempts within assigned locations`, `DM-TRUST-003 withholds synthetic sensitive sentinels without disclosure` | `covered` |
| `R8-CORE-012` | Cover direct and sharded Unprojected Operations with aggregate root state, publication-safe opaque identities, sensitive routing-value withholding without disclosure, generation failure when no safe identity or location exists, and unrelated-marker non-blocking behavior. | README §3.2 Unprojected Operations grammar; README §3.6 publication safety; README §6.1 selected-operation readiness | `unprojected-direct-multibyte-sensitive-valid`; `unprojected-sharded-retrieval-valid`; `unprojected-source-sensitive-valid` | `unprojected-direct-byte-length-invalid`; `unprojected-sharded-group-split-invalid`; `unprojected-source-collision-invalid` | `DM-IDX-008`; `DM-IDX-009`; `DM-IDX-010`; `DM-INC-003` | `core-corpus.test.mjs`: `executes the Task 9 Unprojected Operations corpus and fixes audit retrieval`; `document-set.test.mjs`: direct/sharded Unprojected grammar and retrieval tests, `records DM-IDX-008 source-aware generation-failure and sensitive-withholding expectations separately` | `partial`: focused cases do not yet cover missing publication-safe identity/location, and selected-operation readiness still treats an unrelated direct Unprojected marker in `INDEX.md` as blocking |

## Remaining Core Inventory

The remaining clauses in the README §8 Core corpus paragraphs are intentionally not marked covered by this checkpoint. Subsequent matrix checkpoints append rows for perspective and message selection, messages and payloads, replies and failures, adapters and publication safety, and the additional media-type, source-shard, language, deviation, and conventions requirements. `R8-CORE-012` remains partial until its two named focused-corpus and selected-readiness gaps are resolved. `R8-CORE-001` remains `partial` until every Core clause has a `covered` row and the release review gate confirms that no clause is missing.
