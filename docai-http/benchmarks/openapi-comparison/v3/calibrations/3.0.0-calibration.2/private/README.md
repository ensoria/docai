# Private Calibration 2 Artifacts

This directory is intentionally ignored except for this file. Canonical prompt
records and context metrics are generated locally below `prompts/` and
`contexts/`; later approved work may retain provider evidence below this
directory. Private artifacts are not execution authorization.
