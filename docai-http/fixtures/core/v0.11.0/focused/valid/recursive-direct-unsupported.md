# valid: direct recursive schema unsupported

Expected: valid response representation replacement for a directly recursive source schema.

```markdown
### Response 200

**body_presence**: always

**unsupported**: replaces response representation 200 application/json: directly recursive schema at source/recursive-direct-openapi.yaml#/components/schemas/Node
```
