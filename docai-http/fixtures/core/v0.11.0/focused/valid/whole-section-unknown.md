# valid: whole-section unknown placement

Expected: valid whole-section `unknown` immediately followed by its marker.

```markdown
### Response 200

unknown

**unknown**: response body and headers are not documented; requires source response contract for GET /reports/{id}
```
