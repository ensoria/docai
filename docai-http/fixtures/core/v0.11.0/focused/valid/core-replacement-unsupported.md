# valid: core response representation replacement unsupported

Expected: valid replacement `unsupported` for an unrepresentable JSON response representation.

```markdown
### Response 200

**body_presence**: always

**unsupported**: replaces response representation 200 application/json: directly recursive schema at fixtures/core-openapi.yaml#/components/schemas/Node

- Response Headers: none
```
