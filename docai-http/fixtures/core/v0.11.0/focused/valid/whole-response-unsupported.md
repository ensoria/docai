# valid: whole-response unsupported placement

Expected: valid whole-response replacement `unsupported` directly under the response heading.

```markdown
### Response default

**unsupported**: replaces Response default: mixed error and non-error outcome at fixtures/core-openapi.yaml#/paths/~1jobs/get/responses/default
```
