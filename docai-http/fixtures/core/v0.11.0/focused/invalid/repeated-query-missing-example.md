# invalid: repeated query parameter missing encoded example

Expected: invalid. Repeated query parameters need an encoded example.

```markdown
| Name | Type | Required | Constraints / Meaning |
|---|---|---|---|
| tag | string[] | no | repeated query parameter occurrences; order insignificant |
```
